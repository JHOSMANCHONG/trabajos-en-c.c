#include <math.h>
#include <stdio.h>

struct Coordenada{
    int x;
    int y;
};
int main (){
    int opc;
    printf ("%s\n","seleccionar una opc: ");
        printf ("%s\n","1. Cuadrado");
        printf ("%s\n","2. Triangulo");
        scanf("%d", &opc);
        switch(opc){
            case 1://cuadrado
            struct Coordenada arreglo[4];
            for (int posicion i=0; i<4; i++){
                printf("%s%d\n","ingrese cooredenada x", posicion);
                scanf("%[^\n]", arreglo[i].x);
            printf("%s%d\n","ingrese coordenada y", posicion);
            scanf("%[^\n]", arreglo[i].y);


            }
            float ab=sqrt(
                pow (arreglo)
                printf
            )
        }
}